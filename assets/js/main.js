// Generated by CoffeeScript 1.11.1
(function() {
  var $body, $script_area, $style_elem, __js, _body_selection, _code_block, _codes, _colors, _current_code, _gutter, _script_area, _style_elem, _throttle, _version, _writing_rate, createElement, getURLParam, openComment, openInteger, openString, prevAsterisk, prevSlash, writeChar, writeChars;

  _gutter = 50;

  _writing_rate = 0;

  _throttle = {
    start: 4821,
    stop: 5344
  };

  _colors = {
    offblack: "dark gray",
    background: "#111111",
    text: "#f9f9f9",
    console: "#1F4D6A",
    dark: "#B69D27",
    selector: "#a6da27",
    key: "#64d9ef",
    value: "#fefefe",
    hex: "#f92772",
    text: "#fefefe",
    string: "#d2cc70",
    "var": "#66d9e0",
    operator: "#f92772",
    method: "#f9245c",
    integer: "#fd971c",
    run: "#ae81ff"
  };

  _body_selection = "document.body";

  _current_code = 0;

  _codes = ["* {\n  transition: all 1s;\n}\nbody {\n  background-color: " + _colors.background + ";\n  color: " + _colors.text + ";\n  font-size: 13px;\n  line-height: 1.4;\n  margin: 0;\n  -webkit-font-smoothing: subpixel-antialiased;\n}\n~\`\nvar container = document.createElement(\"div\");\ncontainer.id = \"container\";\n" + _body_selection + ".appendChild(container);\nvar title = document.createElement(\"h1\");\ntitle.id = \"title\";\ntitle.innerHTML = \"Hamza Khan\";\ncontainer.appendChild(title);\n ~\n\`\n#title {\n  position: fixed; width: 100%; \n  top: 0; left: 0; right: 0;\n  font-size: 70px; line-height: 1;\n  font-weight: 100; text-align: center;\n  padding: 10px; margin: 0;\n  z-index: 10;\n  background-color: " + _colors.offblack + ";\n  border-top: 1px solid rgba(255,255,255,0.2);\n  transition: opacity 500ms;\n  font-family: Arial;\n}\n#title em { \n  font-style: normal;\n  color: " + _colors.integer + ";\n}\n~\`\nvar projects = document.createElement(\"div\");\nprojects.id = \"projects\";\ncontainer.appendChild(projects);\n ~\n\`\n#projects {\n  position: absolute; \n  bottom: 70vw;\n  left: 1vw;\n  right: 70vw;\n  top: 70px; \n}\n\n#projects {\n  bottom: 0vw;\n  right: 1vw;\n}\n"];

  $body = document.getElementsByTagName("body")[0];

  createElement = function(tag, id) {
    var el;
    el = document.createElement(tag);
    if (id) {
      el.id = id;
    }
    return el;
  };

  _style_elem = createElement("style", "style-elem");

  _script_area = createElement("div", "script-area");

  $body.appendChild(_style_elem);

  $body.appendChild(_script_area);

  $style_elem = document.getElementById("style-elem");

  $script_area = document.getElementById("script-area");

  openComment = false;

  openInteger = false;

  openString = false;

  prevAsterisk = false;

  prevSlash = false;

  __js = false;

  _code_block = "";

  writeChar = function(which) {
    var char, prior_block_match, prior_comment_match, script_tag;
    if (which === "`") {
      which = "";
      __js = !__js;
    }
    if (__js) {
      if (which === "~" && !openComment) {
        script_tag = createElement("script");
        prior_comment_match = /(?:\*\/([^\~]*))$/;
        prior_block_match = /([^~]*)$/;
        if (_code_block.match(prior_comment_match)) {
          script_tag.innerHTML = _code_block.match(prior_comment_match)[0].replace("*/", "") + "\n\n";
        } else {
          script_tag.innerHTML = _code_block.match(prior_block_match)[0] + "\n\n";
        }
        $script_area.innerHTML = "";
        $script_area.appendChild(script_tag);
      }
      char = which;
    } else {
      char = which === "~" ? "" : which;
      $style_elem.innerHTML += char;
    }
    prevAsterisk = which === "*";
    prevSlash = (which === "/") && !openComment;
    openInteger = which.match(/[0-9]/) || (openInteger && which.match(/[\.\%pxems]/)) ? true : false;
    if (which === '"') {
      openString = !openString;
    }
    return _code_block += which;
  };

  writeChars = function(message, index, interval) {
    if (index < message.length) {
      if (index >= _throttle.start && index < _throttle.stop) {
        interval = 2;
      } else {
        interval = _writing_rate;
      }
      writeChar(message[index++]);
      return setTimeout((function() {
        return writeChars(message, index, interval);
      }), interval);
    }
  };

  getURLParam = function(key, url) {
    var match;
    if (typeof url === 'undefined') {
      url = window.location.href;
    }
    match = url.match('[?&]' + key + '=([^&]+)');
    if (match) {
      return match[1];
    } else {
      return 0;
    }
  };

  _version = getURLParam("billy");

  writeChars(_codes[_version], 0, _writing_rate);

}).call(this);
